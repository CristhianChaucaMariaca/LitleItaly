extends ../../templates/template.pug
include ../../mixins/plato
append config
    // TODO: * modificar variable objeto
    each plato in sopas.content
        // TODO: * modificar variable TITULO
        if(plato.text == "Sopa Minestrone")
            - page = plato.text
            - metadescripcion = plato.description
block content
    include ../../includes/header-plate
    .content__plate
        // TODO: * modificar variable objeto
        each plato in sopas.content
            // TODO: * modificar variable TITULO
            if(plato.text == "Sopa Minestrone") 
                +plato(plato.img,plato.text,plato.price_uno,plato.price_dos,plato.price_tres,plato.description,sopas.category,sponsors[2])
            
    include ../../blocks/btn_atras
    //- TODO: * Modificar variable
    +btn(sopas.category)
    include ../../includes/footer
extends ../../templates/template.pug
include ../../mixins/plato
append config
    // TODO: * modificar variable objeto
    each plato in combos.content
        // TODO: * modificar variable TITULO
        if(plato.text == "Combo 1")
            - page = plato.text
            - metadescripcion = plato.description
block content
    include ../../includes/header-plate
    .content__plate
        // TODO: * modificar variable objeto
        each plato in combos.content
            // TODO: * modificar variable TITULO
            if(plato.text == "Combo 1") 
                +plato(plato.img,plato.text,plato.price_uno,plato.price_dos,plato.price_tres,plato.description,combos.category,sponsors[0])
            
    include ../../blocks/btn_atras
    //- TODO: * Modificar variable
    +btn(combos.category)
    include ../../includes/footer